module.exports = {
  symbols: {
    implements: require('./symbols/implements'),
    requires: require('./symbols/requires')
  },
  implements: require('./implements'),
  requires: require('./requires'),
  registry: require('./registry')
}
